# Voice Chatbot Backend - Python Dependencies

# FastAPI and server
fastapi==0.109.2
uvicorn[standard]==0.27.1
python-multipart==0.0.9

# Text-to-Speech (Google TTS - free)
gTTS==2.5.1

# OpenAI SDK for Whisper (speech-to-text) and GPT (LLM)
# Also compatible with Groq for LLM
openai>=2.0.0

# Utilities
python-dotenv==1.0.1
aiofiles==23.2.1

# Note: Removed SpeechRecognition (incompatible with Python 3.13)
# Now using OpenAI Whisper API directly - much better quality!
